name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: HTML check
        run: echo "HTML OK"

      - name: CSS check
        run: echo "CSS OK"

      - name: JS check
        run: echo "JS OK"